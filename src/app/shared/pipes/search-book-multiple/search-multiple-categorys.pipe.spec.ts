import { SearchMultipleCategorysPipe } from './search-multiple-categorys.pipe';

describe('SearchMultipleCategorysPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMultipleCategorysPipe();
    expect(pipe).toBeTruthy();
  });
});
